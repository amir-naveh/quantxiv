# Next Step

## What
Initialize a Next.js (TypeScript) application as the quantxiv frontend/backend foundation.

## Why
All core features (paper feed, paper pages, comments, auth) require a running web application — this is the prerequisite for everything else.

## Steps
1. Scaffold a Next.js 14 app with TypeScript and Tailwind CSS in a `web/` subdirectory
2. Verify the dev server starts and the default page loads
3. Remove boilerplate content, replace with a minimal quantxiv placeholder page (name + tagline)
